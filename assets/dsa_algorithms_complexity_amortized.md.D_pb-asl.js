import{_ as t,o,c as e,a4 as n}from"./chunks/framework.BRzJ4ijD.js";const u=JSON.parse('{"title":"摊还分析入门","description":"","frontmatter":{},"headers":[],"relativePath":"dsa/algorithms/complexity/amortized.md","filePath":"dsa/algorithms/complexity/amortized.md","lastUpdated":1788475713000}'),s={name:"dsa/algorithms/complexity/amortized.md"};function i(l,a,d,r,p,c){return o(),e("div",null,[...a[0]||(a[0]=[n(`<h1 id="摊还分析入门" tabindex="-1">摊还分析入门 <a class="header-anchor" href="#摊还分析入门" aria-label="Permalink to &quot;摊还分析入门&quot;">​</a></h1><h2 id="一句话" tabindex="-1">一句话 <a class="header-anchor" href="#一句话" aria-label="Permalink to &quot;一句话&quot;">​</a></h2><p>单次操作代价巨大不代表整体慢——只要&quot;贵&quot;的次数被&quot;便宜&quot;的次数均摊过来，平均仍可控。摊还分析就是把这种&quot;凭直觉&quot;的统计变成<strong>形式证明</strong>。它是后面所有动态数组、并查集、Splay 树、Bloom 滤波等结构的工程性能证明语言。</p><h2 id="为什么不能用纯最坏复杂度" tabindex="-1">为什么不能用纯最坏复杂度 <a class="header-anchor" href="#为什么不能用纯最坏复杂度" aria-label="Permalink to &quot;为什么不能用纯最坏复杂度&quot;">​</a></h2><p>考虑动态数组的 <code>push_back</code>：</p><ul><li>大部分时间是 O(1)（写入下一空槽）；</li><li>满了就要扩容 O(n)：申请新数组、拷贝 n 个元素、释放旧数组。</li></ul><p>如果你只看&quot;最坏&quot;，会得到 <strong>push 是 O(n)</strong>。代入 N 次 push 的和：<code>O(N · n) = O(N²)</code>。 但<strong>直觉</strong>告诉我们 N 次 push 在 Θ(N) 量级。证明需要等会一种&quot;统计序列&quot;的工具——摊还分析。</p><p>之所以这件事重要，是因为工程上你<strong>几乎从不</strong>在 N 次 push 都打到 O(n) 最坏。但你要证明这一点，需要把&quot;实际代价序列求和除以操作数&quot;写成一个数学上可证的程序。</p><h2 id="三种方法" tabindex="-1">三种方法 <a class="header-anchor" href="#三种方法" aria-label="Permalink to &quot;三种方法&quot;">​</a></h2><h3 id="_1-聚集法-aggregate-先求全部-再除以次数" tabindex="-1">1. 聚集法（Aggregate）：先求全部，再除以次数 <a class="header-anchor" href="#_1-聚集法-aggregate-先求全部-再除以次数" aria-label="Permalink to &quot;1. 聚集法（Aggregate）：先求全部，再除以次数&quot;">​</a></h3><p>最朴素做法：把所有 n 次操作的总代价求和，再除以 n。</p><p><strong>动态数组倍增</strong>继续上节的节奏：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>扩容发生在 size=1, 2, 4, 8, …</span></span>
<span class="line"><span>每次扩容的开销 = 旧 size（拷贝）+ 1（写新元素）</span></span>
<span class="line"><span>总开销 = 1 + (1+2) + (1+4) + (1+8) + … + (1+n/2)</span></span>
<span class="line"><span>       ≈ n + n/2 + n/4 + … </span></span>
<span class="line"><span>       = 2n ≈ 2n</span></span>
<span class="line"><span>加上每次 push 本身的 1 次 write：+ n</span></span>
<span class="line"><span>总和 ≈ 3n。</span></span>
<span class="line"><span>摊还到每次：Θ(1)。</span></span></code></pre></div><p>简单，但<strong>不能精细区分每个操作的代价</strong>。</p><h3 id="_2-会计法-banker-给便宜操作-预存-贵的时候花" tabindex="-1">2. 会计法（Banker）：给便宜操作&quot;预存&quot;，贵的时候花 <a class="header-anchor" href="#_2-会计法-banker-给便宜操作-预存-贵的时候花" aria-label="Permalink to &quot;2. 会计法（Banker）：给便宜操作&quot;预存&quot;，贵的时候花&quot;">​</a></h3><p>给每次便宜操作<strong>多记一点</strong>当信用：</p><ul><li><code>push</code> 实际花 1，记账 3；</li><li>多余 2 进账户；</li><li>当扩容发生，n 个元素搬迁要花 n，正好从账户里掏（账户此时余额 ≥ 2·(n/2) = n）。</li></ul><p>账户永不为负 ⇒ 每次操作<strong>实际累计 ≤ 记账累计 = 3n</strong> ⇒ 摊还 O(1)。</p><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p>会计法的好处：能给不同操作<strong>不同代价预算</strong>。后面的并查集、链表反转都能用，而且比聚集法精细。</p></div><h3 id="_3-势能法-potential-物理直觉的可迁移方法" tabindex="-1">3. 势能法（Potential）：物理直觉的可迁移方法 <a class="header-anchor" href="#_3-势能法-potential-物理直觉的可迁移方法" aria-label="Permalink to &quot;3. 势能法（Potential）：物理直觉的可迁移方法&quot;">​</a></h3><p>定义势能函数 <code>Φ(i) = Φ(D_i)</code>，描述第 i 次操作后的&quot;数据结构能量&quot;. 要求：</p><ul><li><code>Φ(0) = 0</code>；</li><li>对所有 <code>i ≥ 0</code>，<code>Φ(i) ≥ 0</code>.</li></ul><p>定义第 i 次操作的<strong>摊还代价</strong>：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>a_i = c_i + Φ(D_i) − Φ(D_{i-1})</span></span></code></pre></div><p>求和：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Σ a_i = Σ c_i + Φ(D_n) − Φ(0)</span></span>
<span class="line"><span>       ≥ Σ c_i        （因为 Φ(D_n) ≥ 0 = Φ(0)）</span></span></code></pre></div><p>也就是说<strong>如果你能给所有 a_i 一个上界 k，那 Σc_i ≤ k·n</strong>. 这是最有工程性、最可移植的方法。<strong>记住势能法，所有结构摊还证明你都能用</strong>.</p><h2 id="用势能法重证动态数组-o-1-摊还" tabindex="-1">用势能法重证动态数组 O(1) 摊还 <a class="header-anchor" href="#用势能法重证动态数组-o-1-摊还" aria-label="Permalink to &quot;用势能法重证动态数组 O(1) 摊还&quot;">​</a></h2><p>先试一个朴素势能：<code>Φ(i) = 2·i − cap_i</code>，其中 <code>i</code> 是当前元素数、<code>cap_i</code> 是容量。</p><ul><li><strong>普通 push 不触发扩容</strong>：实际 1，<code>ΔΦ = 2</code>，摊还 <code>3</code>. OK.</li><li><strong>触发扩容 push</strong>：扩容前 cap = i-1，即&quot;之前刚好满 i-1 个的位置再加一个&quot;. 实际开销 <code>= i</code> (拷贝旧 i-1 个 + 写新元素).</li></ul><p>按上面 <code>Φ(i-1) = 2(i-1) − cap_{i-1}</code>，cap_{i-1} 在扩容前 = i-1 ⇒ <code>Φ(i-1) = i-2</code>. 扩容后 cap = 2·cap_old = 2(i-1)，新 i 个元素 ⇒ <code>Φ(i) = 2i − 2(i-1) = 2</code>. ΔΦ = <code>2 − (i-2) = 4 − i</code>.</p><p>摊还 = <code>i + (4 − i) = 4</code>. 这跟上面普通情形的 3 不匹配，但仍为常数。所以<strong>两种情形都是常数</strong> ⇒ 每次插入摊还 O(1)，比例常数 4.</p><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>在势能法里，定义一个新势能函数的&quot;调参&quot;环节通常要试 2-3 次。一旦找到合适的势能函数，结果立刻便宜十倍。这是数学工程的味道。</p></div><h2 id="经典案例速查-直觉" tabindex="-1">经典案例速查 + 直觉 <a class="header-anchor" href="#经典案例速查-直觉" aria-label="Permalink to &quot;经典案例速查 + 直觉&quot;">​</a></h2><table tabindex="0"><thead><tr><th>结构</th><th>实际最坏</th><th>摊还</th><th>势能常见定义</th></tr></thead><tbody><tr><td>动态数组 push</td><td>Θ(n)</td><td>Θ(1)</td><td><code>2i − cap</code></td></tr><tr><td>二进制计数器 +1</td><td>Θ(k) bit 翻</td><td>Θ(1)</td><td><code>#1-bits</code></td></tr><tr><td>Splay 树操作</td><td>Θ(n)</td><td>Θ(log n)</td><td><code>Σ log(size(v))</code></td></tr><tr><td>并查集 find（路径压缩 + 按秩）</td><td>Θ(α(n)) 极慢反例</td><td>实际 ~4 ops</td><td>联集势能（计分函数）</td></tr><tr><td>哈希表 rehash</td><td>Θ(n)</td><td>Θ(1)</td><td>装填因子的惩罚</td></tr></tbody></table><p><strong>二进制计数器</strong>是一个非常直观的势能法例子：n 次 +1 中，最低位每两次翻一次（n/2 次），第二位每四次（n/4 次）……总位数翻转 = <code>n/2 + n/4 + … = O(n)</code> ⇒ 每次摊还 O(1). 你甚至能看到&quot;低位翻得多、高位翻得少&quot;的频次，这就是势能法的心脏直觉。</p><h2 id="splay-树-势能法处理的关键案例" tabindex="-1">Splay 树：势能法处理的关键案例 <a class="header-anchor" href="#splay-树-势能法处理的关键案例" aria-label="Permalink to &quot;Splay 树：势能法处理的关键案例&quot;">​</a></h2><p>Splay 树没有显式平衡。当访问一个节点 x 时，会通过 zig/zig-zig/zig-zag 把它旋转到根。单次最坏 O(n)。但势能法证明：</p><p>定义 <code>Φ(D) = Σ_v log(size(v))</code>，并证明<strong>任何操作摊还 O(log n)</strong>. 核心是 zig/zig-zig 的几何效应：每次 splay 操作都会把访问路径上&quot;重子树 → 轻子树&quot;翻转一次，这种翻转产生<strong>势能释放</strong>直接抵掉操作的代价。</p><p>工程含义：访问过的 key 会被旋转到根，<strong>采样命中率高时变成&quot;自适应 cache&quot;</strong> —— 这就是 Splay 在 Windows 内核里被 Linux CFS 替代前用过一阵的原因。</p><h2 id="并查集摊还-反阿克曼" tabindex="-1">并查集摊还 = 反阿克曼 <a class="header-anchor" href="#并查集摊还-反阿克曼" aria-label="Permalink to &quot;并查集摊还 = 反阿克曼&quot;">​</a></h2><p>只要按秩合并（rank merge）+ 路径压缩，第 n 次 find 的摊还 = <code>O(α(n))</code>，其中 α 是 <strong>逆 Ackermann 函数</strong>——n ≤ 10⁸⁰ 时 α(n) ≤ 4。实际操作几乎常数。</p><p>证明思路非常深（Tarjan 1975），需要：</p><ul><li>把每个节点按 rank 分块；</li><li>在每块内分析 find &quot;跨块&quot;的次数；</li><li>推跨块的多项式级数。</li></ul><p>要在工程里读这个证明很累，但结论一句话：<strong>并查集的每次操作 ≈ 4 条指令的真实代价</strong>——比平衡树 find 小得多。</p><h2 id="工程视角-摊还分析的两个不适用区" tabindex="-1">工程视角：摊还分析的两个不适用区 <a class="header-anchor" href="#工程视角-摊还分析的两个不适用区" aria-label="Permalink to &quot;工程视角：摊还分析的两个不适用区&quot;">​</a></h2><ol><li><p><strong>硬实时系统</strong>。摊还 O(1) 的意思是&quot;长序列平均 O(1)&quot;，并不保证每次都 O(1). 反例：动态数组在第 2^23 个 push 触发扩容，最坏延迟可能数 ms（如果页耗尽需要 syscall 触发 mmap/munmap —— 让 hypervisor 介入甚至给物理页做 numa 分配），HFT/游戏 tick 容不下这种尖刺。</p><p>解法：</p><ul><li>启动时 <code>reserve(max)</code>；</li><li>或换固定容量的 ring buffer；</li><li>或换 incremental rehash（dictugador 风格，把扩容代价分摊到多次 op）。</li></ul></li><li><p><strong>失败重试场景</strong>。如果 N 次 push 里中间某次扩容由于 OOM 而失败，整个数据结构状态可能不一致了。这是 C++ <code>std::vector::push_back</code> 强 EH 保证要求的根源：扩容用临时节点 + commit，不破坏原数据。</p></li></ol><h2 id="这一章带走的东西" tabindex="-1">这一章带走的东西 <a class="header-anchor" href="#这一章带走的东西" aria-label="Permalink to &quot;这一章带走的东西&quot;">​</a></h2><ul><li>平均 vs 摊还 vs 最坏三个量级，<strong>和实际系统行为不能直接对调</strong>；</li><li>势能法是&quot;工程化&quot;的摊还分析，<strong>学会就解锁了一切摊还结构</strong>；</li><li>动态数组的常数是 3，不是抽象的 1；</li><li>Splay 树、并查集、rehash、二进制计数器都靠&quot;势能爆发&quot;在 O(1) 里隐藏 O(n) 的烧火；</li><li>当心两种工程坑：硬实时系统的尖刺、失败回滚下的结构一致性。</li></ul><p>下一节 → <a href="./practice">实战：复杂度反推设计</a>：把 O(·) 从理论用到工程约束反推。</p>`,50)])])}const g=t(s,[["render",i]]);export{u as __pageData,g as default};
