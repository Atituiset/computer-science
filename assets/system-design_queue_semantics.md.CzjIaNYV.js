import{_ as a,o as s,c as t,a4 as i}from"./chunks/framework.BRzJ4ijD.js";const h=JSON.parse('{"title":"消息队列语义","description":"","frontmatter":{},"headers":[],"relativePath":"system-design/queue/semantics.md","filePath":"system-design/queue/semantics.md","lastUpdated":1788475713000}'),n={name:"system-design/queue/semantics.md"};function o(r,e,l,c,p,d){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="消息队列语义" tabindex="-1">消息队列语义 <a class="header-anchor" href="#消息队列语义" aria-label="Permalink to &quot;消息队列语义&quot;">​</a></h1><h2 id="tl-dr" tabindex="-1">TL;DR <a class="header-anchor" href="#tl-dr" aria-label="Permalink to &quot;TL;DR&quot;">​</a></h2><p><strong>&quot;Exactly Once Delivery&quot; 是不可能由消息队列</strong> 单独保证 (除非队列做单 consumer 组合 + idempotent 处理)。 三 fantasy:</p><ol><li><strong>At-most-once</strong>: producer 发出而 没收到 ack, message 可能未送达 consumer; consumer 可能没收到; <strong>可能丢失</strong>。</li><li><strong>At-least-once</strong>: producer 必重试 <strong>直到 acked</strong>, consumer 必重认. <strong>可能收多次重复消息</strong>, idempotent processing 必可 bear.</li><li><strong>Exactly-once</strong>: 应用层保证 — consumer 幂等 + dedup, queue 提供单 partition ordering + offset commit.</li></ol><p>工业实际至少 是 <strong>at-least-once + idempotent consumer</strong>. 标语 &quot;exactly-once&quot; 实际是 &quot;exactly-once <em>effects</em>&quot; through client-side deduplication. 本章扫 三种语义的协议细节, Kafka/Pulsar/NATS/SQS 公布的语义, 端到端 idempotency 设计 (idempotency key + dedup table + outbox pattern), 与典型事故 (双 at-least-once linked transfer 双扣款)。</p><hr><h2 id="一、形式化三种语义" tabindex="-1">一、形式化三种语义 <a class="header-anchor" href="#一、形式化三种语义" aria-label="Permalink to &quot;一、形式化三种语义&quot;">​</a></h2><h3 id="at-most-once-丢消息容忍" tabindex="-1">At-most-once (丢消息容忍) <a class="header-anchor" href="#at-most-once-丢消息容忍" aria-label="Permalink to &quot;At-most-once (丢消息容忍)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>producer.send(msg):</span></span>
<span class="line"><span>   发出 -&gt; 没 ack (fire-and-forget).</span></span>
<span class="line"><span></span></span>
<span class="line"><span>consumer.receive(msg):</span></span>
<span class="line"><span>   process(msg); ack;</span></span>
<span class="line"><span>   network fail -&gt; 不重试来 → msg lost (MOST- ONCE 上限 receive一次。</span></span></code></pre></div><ul><li>用例: telemetry, events log, real weather readings, fire-and-forget 通知 push 通知 (无 critical).</li><li>实现: UDP 或 应用层 no ack. SQS exposes this 不 默认.</li></ul><h3 id="at-least-once-有-duplicates-可能" tabindex="-1">At-least-once (有 duplicates 可能) <a class="header-anchor" href="#at-least-once-有-duplicates-可能" aria-label="Permalink to &quot;At-least-once (有 duplicates 可能)&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>producer.send(msg):</span></span>
<span class="line"><span>   if no ack in timeout -&gt; retry.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>consumer.receive(msg):  </span></span>
<span class="line"><span>   process(msg); ack;</span></span>
<span class="line"><span>   if crash before ack -&gt; 重新 deliver to consumer/broker。</span></span></code></pre></div><ul><li>用例: 大多 工 业 message queues (Kafka, RabbitMQ, Pulsar, NATS). default.</li><li>duplicate 要求 consumer 端是 <strong>idempotent</strong>.</li></ul><h3 id="exactly-once-效果上" tabindex="-1">Exactly-once (效果上) <a class="header-anchor" href="#exactly-once-效果上" aria-label="Permalink to &quot;Exactly-once (效果上)&quot;">​</a></h3><ul><li>Producer side: 不 重 复 (<strong>idempotent producer</strong>) Kafka 0.11+ <code>enable.idempotence=true</code>. 每个 batch 由 producer_id+seq_no 去重.</li><li>Consumer side: <strong>transactional消费 + commit一 offset</strong>, Kafka transactions 进 offset write 与 log append在同一事务. consumer <code>read_committed</code> 后 only see committed.</li><li>Effects: consumer 必 <strong>idempotent</strong> + queue 提 供 idempotence + transactional support.</li></ul><p><strong>This is the practical exactly-once 定义</strong> — actual 1 time delivery 不保证, 但 1 time effects 保证. Network partition can still cause consumer to process twice in failure recovery; idempotency ensure action 一次.</p><h3 id="true-once-vs-effective-once" tabindex="-1">&quot;True once vs effective once&quot; <a class="header-anchor" href="#true-once-vs-effective-once" aria-label="Permalink to &quot;&quot;True once vs effective once&quot;&quot;">​</a></h3><p>Layer 1 delivers at-least-once + Layer 2 (consumer-side idempotency) ⇒ effective once:</p><ul><li>idempotency key (unique business id)</li><li>dedup store (Redis SETNX with TTL or DB unique key constraint)</li><li>transactional update to multiple systems (projection update)</li></ul><hr><h2 id="二、kafka-语义" tabindex="-1">二、Kafka 语义 <a class="header-anchor" href="#二、kafka-语义" aria-label="Permalink to &quot;二、Kafka 语义&quot;">​</a></h2><h3 id="producer-配置" tabindex="-1">Producer 配置 <a class="header-anchor" href="#producer-配置" aria-label="Permalink to &quot;Producer 配置&quot;">​</a></h3><ul><li><code>acks=0</code>: at-most-once (fire and forget).</li><li><code>acks=1</code>: leader writes 后 ack; if leader fail before ISR 同步 → msg lost.</li><li><code>acks=-1 / all</code>: leader + 全 ISR 同步 后 ack; RPO ≈ 0 with min.insync.replicas=2 + replication factor=3. <strong>At-least-once</strong> default since Kafka 3.0.</li><li><code>enable.idempotence=true</code> (Kafka 3.0+ default): producer 内分配 <code>producer_id</code> + sequence number per partition, broker side 去重. <strong>Exactly-once send</strong> (无 duplicate).</li></ul><h3 id="consumer" tabindex="-1">Consumer <a class="header-anchor" href="#consumer" aria-label="Permalink to &quot;Consumer&quot;">​</a></h3><p>Kafka consumer 提供 &quot;<strong>at-least-once</strong>&quot; delivery: per-partition offset commit:</p><ul><li><code>enable.auto.commit=true</code> + <code>auto.commit.interval.ms=5000</code>: 自 commit 后 consumer crash 之前 实际 processing 可能 never finish干, 但 commit 已 send → loss.</li><li><code>enable.auto.commit=false</code> + manual-sync commit After processing: duplicates possible if crash 后 process 后 before commit. ** Idempotent consumer required.**</li></ul><h3 id="transactions-kafka-0-11" tabindex="-1">Transactions (Kafka 0.11+) <a class="header-anchor" href="#transactions-kafka-0-11" aria-label="Permalink to &quot;Transactions (Kafka 0.11+)&quot;">​</a></h3><p>Producer 以 transactional <code>initTransactions()</code> 在 Kafka注册 <code>transactional.id</code>, 与 read-process-write transactional pipeline:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>producer.beginTransaction()</span></span>
<span class="line"><span>producer.send(topic_a, msg_out_1)</span></span>
<span class="line"><span>producer.send(topic_b, msg_out_2)</span></span>
<span class="line"><span>producer.sendOffsetsToTransaction(consumer_offsets)         # commit consumer offsets in same Txn</span></span>
<span class="line"><span>producer.commitTransaction()</span></span></code></pre></div><p>让 consumer 链路 transaction 与其 read commit offset 都在一个 Kafka事务 中. 两件事原子。下 consumer <code>isolation.level=read_committed</code> 不见未 commit 数据 → 不重复读取。</p><h3 id="kafka-stream-ktable" tabindex="-1">Kafka Stream / kTable <a class="header-anchor" href="#kafka-stream-ktable" aria-label="Permalink to &quot;Kafka Stream / kTable&quot;">​</a></h3><p>Kafka Streams 内部用 transactions + 实际流处理 + state stores + offset commit 一事务 atomic → exactly-once processing <strong>guaranteed</strong> by lib. 没 idempotent library (像 plain client apps) 必须 ID 也自行 dedup.</p><hr><h2 id="三、pulsar-rabbitmq-sqs-nats-对比" tabindex="-1">三、Pulsar / RabbitMQ / SQS / NATS 对比 <a class="header-anchor" href="#三、pulsar-rabbitmq-sqs-nats-对比" aria-label="Permalink to &quot;三、Pulsar / RabbitMQ / SQS / NATS 对比&quot;">​</a></h2><h3 id="pulsar" tabindex="-1">Pulsar <a class="header-anchor" href="#pulsar" aria-label="Permalink to &quot;Pulsar&quot;">​</a></h3><ul><li>BookKeeper ledger 提供 ledger-level ensemble write 投统一, end-to-end shared storage, 加序号, acked message 可recovered.</li><li>&quot;At-least-once&quot; 默认; &quot;exactly-once&quot; 可以 subscribe unique consumer (shared subscription with name) + dedup at 正确.</li><li><strong>Dedup</strong> with <code>producer_name</code> + <code>sequence_id</code> (Vue 系列 ...) üyes 直接支持</li></ul><h3 id="rabbitmq" tabindex="-1">RabbitMQ <a class="header-anchor" href="#rabbitmq" aria-label="Permalink to &quot;RabbitMQ&quot;">​</a></h3><ul><li>默认 at-least-once, consumer ack manual.</li><li>message TTL, queue-level max size.</li><li><strong>Generally not idempotent</strong> dedup, broker doesn&#39;t store 已处理 IDs. Application must dedupe.</li><li>supports dead-letter exchange 来 without retry with TTL delay.</li></ul><h3 id="aws-sqs" tabindex="-1">AWS SQS <a class="header-anchor" href="#aws-sqs" aria-label="Permalink to &quot;AWS SQS&quot;">​</a></h3><ul><li><strong>At-least-once</strong> default; consumer 处理 + delete API; visibility timeout default 30s 后未 delete → message reap-appears。</li><li><strong>FIFO queue</strong> with deduplication: producer 提供 <code>MessageDeduplicationId</code> → 5 分钟内同 id 抑制 (exactly-once per 5min window). Good for low-volume critical events.</li><li><strong>DLQ (Dead Letter Queue)</strong>: max receive count exceeded → moved to DLQ for later analysis.</li><li>部署 large scale is best practice with FIFO queue + deduplication_id + visibility_timeout 决定.</li></ul><h3 id="nats" tabindex="-1">NATS <a class="header-anchor" href="#nats" aria-label="Permalink to &quot;NATS&quot;">​</a></h3><ul><li><strong>At-most-once</strong> by default (no ack).</li><li>NATS JetStream (newer) 提供 persistent + acks + at-least-once.</li><li>High throughput + log library.</li></ul><hr><h2 id="四、outbox-pattern-→-outbox-md" tabindex="-1">四、Outbox Pattern (→ <a href="./outbox">outbox.md</a>) <a class="header-anchor" href="#四、outbox-pattern-→-outbox-md" aria-label="Permalink to &quot;四、Outbox Pattern (→ [outbox.md](outbox.md))&quot;">​</a></h2><p>外 trans actor 必须 update DB + send message queue atomic. 若 update DB 之后失败 ∈ queue delivery, 没 send. 若 send queue 之后 crash pre db commit, downstream 看到 event 但 DB 没 reflect, 是 service consistency break.</p><p>Outbox pattern 解法:</p><ul><li>DB transaction 中既 插入 <code>_outbox</code> 表 一个 row = event, 与业务 row 同事务.</li><li>异步 worker poll <code>_outbox</code> 表 → 发到 queue → 标记为 sent.</li><li>queue publish + outbox row delete 必须 idempotent (DB unique constraint <code>event_id</code>, queue dedup <code>event_id</code>).</li></ul><p>也支持 CDC (Debezium 监 听 binlog + emit 到 kafka).</p><hr><h2 id="五、幂等-consumer-pattern" tabindex="-1">五、幂等 Consumer Pattern <a class="header-anchor" href="#五、幂等-consumer-pattern" aria-label="Permalink to &quot;五、幂等 Consumer Pattern&quot;">​</a></h2><p>幂等处理 是 at-least-once 工业基本 assume. Consumer 必记 processed event_ids / dedupe.</p><h3 id="db-unique-constraint" tabindex="-1">DB Unique Constraint <a class="header-anchor" href="#db-unique-constraint" aria-label="Permalink to &quot;DB Unique Constraint&quot;">​</a></h3><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> TABLE</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> processed_events</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    event_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">VARCHAR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">PRIMARY KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    processed_at </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">TIMESTAMP</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> DEFAULT</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> NOW</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    payload JSONB</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- Process:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">INSERT INTO</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> processed_events (event_id, payload)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">VALUES</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (?, ?)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CONFLICT (event_id) DO NOTHING</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">RETURNING (xmax </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">AS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> was_inserted;</span></span></code></pre></div><p>如果 insert returns <code>was_inserted=true</code>, 是 first processing. 否则, 已处理, <strong>skip</strong> 但允许 commit/consume.</p><h3 id="redis-setnx-with-ttl" tabindex="-1">Redis SETNX With TTL <a class="header-anchor" href="#redis-setnx-with-ttl" aria-label="Permalink to &quot;Redis SETNX With TTL&quot;">​</a></h3><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">def</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> process_safe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event_id, business_func):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> not</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> redis.set(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;processed:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">{</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">event_id</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">}</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">nx</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">True</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ex</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">86400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;already_processed&quot;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> business_func(event_id)</span></span></code></pre></div><p>short dedupe window via Redis TTL. 适合 bank transfer events.</p><h3 id="idempotency-key-on-business-object" tabindex="-1">Idempotency Key on Business Object <a class="header-anchor" href="#idempotency-key-on-business-object" aria-label="Permalink to &quot;Idempotency Key on Business Object&quot;">​</a></h3><p>Use a stable business identifier as idempotency key:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>transfer request: </span></span>
<span class="line"><span>  idempotency_key = &quot;Transfer|alice|bob|transfer_id_42&quot;</span></span>
<span class="line"><span>  process:</span></span>
<span class="line"><span>    if key seen in records: return prior result</span></span>
<span class="line"><span>    else: perform transfer, store result</span></span></code></pre></div><hr><h2 id="六、典型事故" tabindex="-1">六、典型事故 <a class="header-anchor" href="#六、典型事故" aria-label="Permalink to &quot;六、典型事故&quot;">​</a></h2><h3 id="stripe-duplicate-transfer-双扣款" tabindex="-1">Stripe duplicate transfer (双扣款) <a class="header-anchor" href="#stripe-duplicate-transfer-双扣款" aria-label="Permalink to &quot;Stripe duplicate transfer (双扣款)&quot;">​</a></h3><p>2018 某 Stripe webhook 失败重试一次 service, customer 写 了 paypal — ID错过了而是 一次性 completed trans del Declared result 充枣 高加! Escape 失败 had 时光. 但 hashing request hash 减少 duplicate check format 等配. Fix: <code>Idempotency-Key</code> HTTPS required any writing API.</p><h3 id="kafka-exactly-once-向前-duplicate-2016" tabindex="-1">Kafka exactly-once &quot;向前 duplicate&quot; 2016 <a class="header-anchor" href="#kafka-exactly-once-向前-duplicate-2016" aria-label="Permalink to &quot;Kafka exactly-once &quot;向前 duplicate&quot; 2016&quot;">​</a></h3><p>Some Kafka business batches write to next downstream outside Kafka, 该 downstream RECEIVE 更多 program 复制 message. 应用 transform. Fix: process inside Kafka transactional commit.</p><h3 id="twitter-fanout-tail-event-dedupe" tabindex="-1">Twitter Fanout tail event dedupe <a class="header-anchor" href="#twitter-fanout-tail-event-dedupe" aria-label="Permalink to &quot;Twitter Fanout tail event dedupe&quot;">​</a></h3><p>Each fanout event <code>event_id</code> 逐渐复杂性 同 actual server 引入 Redis SETNX. Hot spot. Fix: hash 各 partition agent id 同 by id, partition placement stable.</p><h3 id="event-queue-from-processing-fail-发送邮件-duplicate" tabindex="-1">Event queue from processing fail 发送邮件 duplicate <a class="header-anchor" href="#event-queue-from-processing-fail-发送邮件-duplicate" aria-label="Permalink to &quot;Event queue from processing fail 发送邮件 duplicate&quot;">​</a></h3><p>发 user emails 后下 系统 处理回 callAck → consumer failures reassigned to worker duplicate. Payment providers往往 3 retries in DLQ. Idempotency ID store 邮件 list 总筛 duplicate.</p><hr><h2 id="七、易错清单" tabindex="-1">七、易错清单 <a class="header-anchor" href="#七、易错清单" aria-label="Permalink to &quot;七、易错清单&quot;">​</a></h2><ol><li><strong>At-least-once 是默认</strong>: 必 定 claim idempotency, 写 client + dedup 是 standard.</li><li><strong>Auto-commit 该 disable</strong>: manual commit after processing 避免丢.</li><li><strong>Kafka producer 的 idempotence + transactional stream</strong> 提供支持 transaction; custom processing 必须应用 idempotency.</li><li><strong>Idempotency key must be stable</strong>: 不要 hash current time, use 业务级 unique ID (e.g., order_id).</li><li><strong>Visibility timeout &gt; max processing latency</strong>: else broker re-deliver original message duplicate. SQS visibility deafault 30s often too short.</li><li><strong>DLQ 监控必</strong>: failures 让 message autonomous bypass manual inspection; silent depute Significant old embed.</li><li><strong>Network partition</strong>: 多 send batch deliverCLient 拉 出 ATTEMPT 船 Rector多种 一次 思成功 2次 首项 准备 likelyhood client下次 写 际修复 logical input 干.</li><li><strong>Ordering与delivery 重</strong>: 实部分 ordering not scaling well. 一 个 partition 是强 order 多 partition 全局 sorted impossible guarantees (sync only).</li></ol><hr><h2 id="八、这一章带走的东西" tabindex="-1">八、这一章带走的东西 <a class="header-anchor" href="#八、这一章带走的东西" aria-label="Permalink to &quot;八、这一章带走的东西&quot;">​</a></h2><ol><li>At-most-once (fire-and-forget), At-least-once (retry until acked), Exactly-once (idempotent + transactional).</li><li>现实生产 多 <strong>At-least-once + Idempotency ID dedup</strong>; Exactly-once 通常 应用层自行实现.</li><li>Kafka producer idempotence + transactions + consumer offsets in transaction 提供 end-to-end true exactly-once.</li><li>SQS FIFO with <code>MessageDeduplicationId</code> + Visibility Timeout; RabbitMQ manual ack; NATS JetStream persistent.</li><li><strong>Outbox Pattern</strong> 让 DB + queue 同 事务 并 atomic; CDC替代 outbox table.</li><li>应用层 idempotency: 用户 + Event ID + Redis SETNX + DB unique constraint <code>event_id</code>.</li></ol><hr><p>下一节 → <a href="./outbox">Outbox Pattern</a></p>`,78)])])}const k=a(n,[["render",o]]);export{h as __pageData,k as default};
