import{_ as a,o as t,c as n,a4 as s}from"./chunks/framework.BRzJ4ijD.js";const h=JSON.parse('{"title":"一致性、线性化与序","description":"","frontmatter":{},"headers":[],"relativePath":"distributed/concepts/ordering.md","filePath":"distributed/concepts/ordering.md","lastUpdated":1788475713000}'),o={name:"distributed/concepts/ordering.md"};function r(i,e,l,c,d,p){return t(),n("div",null,[...e[0]||(e[0]=[s(`<h1 id="一致性、线性化与序" tabindex="-1">一致性、线性化与序 <a class="header-anchor" href="#一致性、线性化与序" aria-label="Permalink to &quot;一致性、线性化与序&quot;">​</a></h1><h2 id="tl-dr" tabindex="-1">TL;DR <a class="header-anchor" href="#tl-dr" aria-label="Permalink to &quot;TL;DR&quot;">​</a></h2><p>分布式系统里&quot;一致性&quot;是多义词——本节梳理<strong>三套区分</strong>:</p><ol><li><strong>线性化 (Linearizability):</strong> 单对象最强的实时一致性——任何看到&quot;前面写入&quot;的客户端都看到该写入, 现代&quot;实时一致&quot;通用代称。</li><li><strong>一致性等级体系:</strong> 从强到弱——Strict(单 processor)→ Linearizable (real-time global)→ Sequential (single global order) → Causal (causally-related ordered)→ Eventual (eventually converges)→ Read-your-writes (per-client)。</li><li><strong>顺序与因果:</strong> Lamport defines <code>happens-before</code> (<code>→</code>) partial order, 由&quot;消息发送/接收, 同进程内序&quot;构造; Vector clock 用一组计数器在 N 节点上捕捉 <code>happens-before</code>, 检测并发但非 total order; HLC (Hybrid Logical Clock) 把 wall clock + logical clock 合一, 兼顾分布式因果和人读时间戳。 理解这后面 Paxos/Raft/CRDT 的取舍能显性化。</li></ol><hr><h2 id="一、线性化-linearizability" tabindex="-1">一、线性化 (Linearizability) <a class="header-anchor" href="#一、线性化-linearizability" aria-label="Permalink to &quot;一、线性化 (Linearizability)&quot;">​</a></h2><h3 id="定义-herlihy-wing-1990" tabindex="-1">定义 (Herlihy &amp; Wing 1990) <a class="header-anchor" href="#定义-herlihy-wing-1990" aria-label="Permalink to &quot;定义 (Herlihy &amp; Wing 1990)&quot;">​</a></h3><p>并发操作 (operations on a single object) 总可看作一个<strong>全序</strong> (a total order), 且该 total order 满足:</p><ol><li><strong>Real-time order</strong>: 若 op1 完成在 op2 开始之前 (real-time), 则 total order 中 op1 必须排在 op2 之前。</li><li><strong>Sequential spec</strong>: total order 是一个<strong>合法的 sequential history</strong>——即, 是单机串行执行的一种可能结果 (e.g., 就是普通读写的语义)。</li></ol><h3 id="直观例子" tabindex="-1">直观例子 <a class="header-anchor" href="#直观例子" aria-label="Permalink to &quot;直观例子&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Client A: write(x, 1) at t=10, ack at t=20</span></span>
<span class="line"><span>Client B: read(x) at t=15, response at t=25  → 必须返回 1 (因为 A 的 read 已 ack)</span></span>
<span class="line"><span>Client C: read(x) at t=18, response at t=22  → 可返回 0 (因 A 尚未 ack)</span></span></code></pre></div><p>线性化的承诺是: 当 A 收到 ack 后, 任何后续 B 的 read 都看到 A 的写。</p><h3 id="线性化-vs-serializability" tabindex="-1">线性化 vs Serializability <a class="header-anchor" href="#线性化-vs-serializability" aria-label="Permalink to &quot;线性化 vs Serializability&quot;">​</a></h3><ul><li><strong>Linearizability</strong>: about 单对象并发; 实时保证。</li><li><strong>Serializability</strong>: 多对象事务; <strong>无</strong>实时保证——只要等价于某个 serial 事务序列即可。</li></ul><p>一个 system 可以是 linearizable 但 non-serializable (空 if use multi-object transactions). 一个 system 可以是 serializable 不 linearizable (e.g., 一个 read-only transaction 可以看到 t=15 写但 t=20 位先 ack 也 OK)。</p><p>Spanner 提供的是 both—— linearizable + serializable together call &quot;strict serializability&quot; 或 &quot;external consistency&quot;。</p><h3 id="测试线性化-jepsen-knoss" tabindex="-1">测试线性化: Jepsen Knoss <a class="header-anchor" href="#测试线性化-jepsen-knoss" aria-label="Permalink to &quot;测试线性化: Jepsen Knoss&quot;">​</a></h3><p>Jepsen 大杀器: 把某次 run 收集全 client operation 的<strong>invocation-completion</strong> pairs, 喂 Knoss 算法——枚举所有可能的 total order 检查是否存在合法的 (线性化)*。 这是 exponential worst case, 但实战上小 model 能验千次 ops.</p><hr><h2 id="二、sequential-consistency-lamport-1979" tabindex="-1">二、Sequential Consistency (Lamport 1979) <a class="header-anchor" href="#二、sequential-consistency-lamport-1979" aria-label="Permalink to &quot;二、Sequential Consistency (Lamport 1979)&quot;">​</a></h2><p>弱化线性化——<strong>取消 real-time 约束</strong>, 只要求一个&quot;看上去各 node 共享的 total order&quot;。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>A: write(x, 1) at t=10, ack at t=15</span></span>
<span class="line"><span>B: read(x) at t=12, return 0</span></span>
<span class="line"><span>C: read(x) at t=20, return 1</span></span></code></pre></div><p>合法: total order <code>B read → A write → C read</code> 各进程看到的序一致, 但 B 的读在 A 的写之前完成 real-time——违反了 linearizability 但 sequential OK.</p><p>Sequential 比 linearizable 弱, 但比 causal 强 (因强加 total order). 现代 CPU memory model 通常仅保证 sequential consistency (且大多是 weak model—— TSO, ARM relaxed).</p><h3 id="cpu-memory-model-类比" tabindex="-1">CPU Memory Model 类比 <a class="header-anchor" href="#cpu-memory-model-类比" aria-label="Permalink to &quot;CPU Memory Model 类比&quot;">​</a></h3><ul><li>x86: TSO (Total Store Order) — 大部分 sequential, 但 store-load 可重排。</li><li>ARM: relaxed — almost any reordering, only data/control deps enforced。</li><li>Java JMM / C++11 memory_order_seq_cst = 严格 sequential consistency。</li></ul><p>线性化是 distributed systems 强一, sequential 是硬件 memory order 强一——同概念不同领域。</p><hr><h2 id="三、causal-consistency" tabindex="-1">三、Causal Consistency <a class="header-anchor" href="#三、causal-consistency" aria-label="Permalink to &quot;三、Causal Consistency&quot;">​</a></h2><h3 id="partial-order-形式化" tabindex="-1">Partial order 形式化 <a class="header-anchor" href="#partial-order-形式化" aria-label="Permalink to &quot;Partial order 形式化&quot;">​</a></h3><p>定义 happens-before <code>→</code>:</p><ul><li>同进程: <code>e1 → e2</code> if <code>e1</code> happens before <code>e2</code> in same process.</li><li>跨进程: <code>send(m) → recv(m)</code> (发出 before 收到)。</li><li>传递: <code>e1 → e2 ∧ e2 → e3 ⟹ e1 → e3</code>。</li><li>否则 concurrent: <code>e1 || e2</code>.</li></ul><p>Causal consistency: 所有 nodes 看到的因果关系<strong>保持序</strong>, 但并发可任任意序。</p><h3 id="vector-clock" tabindex="-1">Vector Clock <a class="header-anchor" href="#vector-clock" aria-label="Permalink to &quot;Vector Clock&quot;">​</a></h3><p>N 个 nodes vector clock <code>VC_i</code> 维护 N-element count:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>on send: VC_i[i]++ then send VC</span></span>
<span class="line"><span>on recv(m, VC_m): VC_i[j] = max(VC_i[j], VC_m[j]) for all j; VC_i[i]++.</span></span></code></pre></div><p>判定: <code>VC_a &lt; VC_b iff VC_a[k] ≤ VC_b[k] for all k AND strictly &lt; for some k</code>.</p><p>并发: <code>VC_a || VC_b</code>(不 ≤ 也不 ≥)。</p><p>工程用: Dynamo, Riak 用 vector clock 检测 concurrently writes。 每个 update 加上 client-side VC; read 时高 VC → 覆盖旧 VC; concurrent VC 全保留 → siblings + merge function。</p><h3 id="version-vector-dotted-version-vector-interval-version-vector" tabindex="-1">Version Vector, Dotted Version Vector, Interval Version Vector <a class="header-anchor" href="#version-vector-dotted-version-vector-interval-version-vector" aria-label="Permalink to &quot;Version Vector, Dotted Version Vector, Interval Version Vector&quot;">​</a></h3><p>演进路径:</p><ul><li><strong>Version Vector (VV)</strong>: N 维, 简单, 但每 writer 必须同步协调 N= nodes 数。</li><li><strong>Dotted VV (DVV)</strong>: 解决 VV 假设全 writer 持有当前 VV, 否则假 sibling; 引入 (dot, cluster) 表达精细化。</li><li><strong>Interval Version Vector (IVV)</strong>: 适应 dynamic membership 变化, when node failure/ add, vector interval 而不是 count。</li></ul><p>Riak 现版用 DVV, Cassandra 用 VV + LWW (Last-Write-Wins) 简化算法。</p><hr><h2 id="四、read-your-writes-consistency-session-guarantees" tabindex="-1">四、Read-Your-Writes Consistency (Session Guarantees) <a class="header-anchor" href="#四、read-your-writes-consistency-session-guarantees" aria-label="Permalink to &quot;四、Read-Your-Writes Consistency (Session Guarantees)&quot;">​</a></h2><p>Terry et al. 1994, Bayou 系统。session 是一个 client 的&quot;使用上下文&quot;, 在 (possibly sticky-load-balancer) 单 server 或一个 quorum 对读写的关联。</p><p>四种 session guarantee:</p><table tabindex="0"><thead><tr><th>Guarantee</th><th>含义</th></tr></thead><tbody><tr><td><strong>Read-Your-Writes (RYW)</strong>: client 看到自己写入</td><td>Web 用户体验基本——购物车追加后看到新项目</td></tr><tr><td><strong>Monotonic Reads</strong>: 后续 reads <strong>看到更近期</strong>的数据</td><td>单调, 不&quot;看到 back-in-time&quot;</td></tr><tr><td><strong>Monotonic Writes</strong>: 一个 client 的<strong>写入的 serializable 总顺序</strong>与自己 process 内一致</td><td>避免 undo user 感受</td></tr><tr><td><strong>Writes-Follow-Reads</strong>: 同一 session 之前读到的值, 后续 writes 在该值基础上</td><td>锁/版本更安全</td></tr></tbody></table><p>工程实现:</p><ul><li>Sticky load balancer 保障 session 一定打到同一副本 (常见但是 bad failover path)</li><li>Read 时含 <strong>client_last_seen_vclock</strong>, server 检查 last-VC 后有的 update 都同步到自身后再响应 client (synchronous read-repair).</li></ul><hr><h2 id="五、eventual-consistency" tabindex="-1">五、Eventual Consistency <a class="header-anchor" href="#五、eventual-consistency" aria-label="Permalink to &quot;五、Eventual Consistency&quot;">​</a></h2><p>最弱: 给足<strong>无新写入</strong> 时间, 所有副本最终 converge (e.g., VC 等效)。<strong>不</strong>提业务可接受的 &quot;时间长度&quot;。AP 系统是 eventual consistency 的如下根源:</p><ul><li>无 quorum: W=1 R=1 N=3 — 允许 client 写出现在另一副本</li><li>gossip/Anti-entropy 同步最终传播</li><li>merkle tree 加速 read repair (Riak/Cassandra)</li></ul><h3 id="eventual-不意味着-等同-linearizable" tabindex="-1">Eventual 不意味着 &quot;等同 linearizable&quot;: <a class="header-anchor" href="#eventual-不意味着-等同-linearizable" aria-label="Permalink to &quot;Eventual 不意味着 &quot;等同 linearizable&quot;:&quot;">​</a></h3><p>Dynamo 测试中, eventual consistency 与 linearizability<strong>绝对不等</strong>——client user 看到 timeline 已 push 后改用旧 timeline 反向 replica, insider sees LWW 取舍了 user complex about time.</p><h3 id="slo-上-eventual-window-多长" tabindex="-1">SLO 上 eventual: window 多长? <a class="header-anchor" href="#slo-上-eventual-window-多长" aria-label="Permalink to &quot;SLO 上 eventual: window 多长?&quot;">​</a></h3><p>DynamoDB Eventually Consistent Read: &lt;1s window 在集团内。Cassandra (~minutes if no read-repair 默认) wallclock 设置 <code>gc_grace_seconds</code> 默认 10 days。</p><hr><h2 id="六、hlc-kulkarni-2014" tabindex="-1">六、HLC (Kulkarni 2014) <a class="header-anchor" href="#六、hlc-kulkarni-2014" aria-label="Permalink to &quot;六、HLC (Kulkarni 2014)&quot;">​</a></h2><h3 id="problem-of-pure-lc" tabindex="-1">Problem of pure LC <a class="header-anchor" href="#problem-of-pure-lc" aria-label="Permalink to &quot;Problem of pure LC&quot;">​</a></h3><p>Lamport Clock 单调 64bit count; 平常很快但<strong>与 wall clock 无关</strong>——读 <code>2024_05_01 10:23:00</code> 看 clock &quot;5&quot; 很反直觉。</p><h3 id="hlc" tabindex="-1">HLC <a class="header-anchor" href="#hlc" aria-label="Permalink to &quot;HLC&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>HLC = (physical_ts, logical_count)</span></span>
<span class="line"><span>init: HLC = (wall_now, 0)</span></span>
<span class="line"><span>on_send/recv:</span></span>
<span class="line"><span>  if wall_now &gt; HLC.physical:</span></span>
<span class="line"><span>     HLC = (wall_now, 0)</span></span>
<span class="line"><span>  else:</span></span>
<span class="line"><span>     HLC.logical += 1</span></span></code></pre></div><p>Vector style 同 vector clock 维护 N 个 HLC——cockroachdb、yugabyte、 mongo cluster 都用 HLC 替代 wall clock 在事务上做 ordering。</p><p>HLC 提供:</p><ol><li><strong>causal consistency</strong> (类似 LC, 自然支持)</li><li><strong>timestamp stability</strong>: 不知道因果, 可以用 PH.ts vs PH&#39;.ts 直接比较</li><li><strong>debug 友好</strong>: timestamp 是 wall clock 大致时间, 排查 bug 容易</li></ol><h3 id="truetime" tabindex="-1">TrueTime <a class="header-anchor" href="#truetime" aria-label="Permalink to &quot;TrueTime&quot;">​</a></h3><p>Spanner 是 Google 一个跨越——不等 HLC, 直接用 GPS/原子钟提供 TrueTime API <code>TT.now() = [earliest, latest]</code> 区间 + commit-wait until upper-bound 确保每 commit 的 ts 严格小于后续 begins。 TrueTime 不是 HLC, 但基础 wall clock 保证 external consistency——这是 Google 的工程极限。</p><hr><h2 id="七、一致性选择光谱" tabindex="-1">七、一致性选择光谱 <a class="header-anchor" href="#七、一致性选择光谱" aria-label="Permalink to &quot;七、一致性选择光谱&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Strong (低 throughput, 高延迟)</span></span>
<span class="line"><span>─────────────────────────────────</span></span>
<span class="line"><span>Strict Serializable        Spanner</span></span>
<span class="line"><span>Serializable + Lin        CockroachDB, YugaByte, FaunaDB</span></span>
<span class="line"><span>Serializable                PostgreSQL (单机)</span></span>
<span class="line"><span>Snapshot Isolation SI      PostgreSQL REPEATABLE READ, Oracle</span></span>
<span class="line"><span>                            ─────┘并行调度 easy</span></span>
<span class="line"><span>Read Committed              PostgreSQL READ COMMITTED, MySQL InnoDB</span></span>
<span class="line"><span>                            ─────┘Panicking jdbc 默认 — 单 statement - level checks</span></span>
<span class="line"><span>Causal Consistency          COPS, Bayesian Stores, TLA+ Casual storage</span></span>
<span class="line"><span>Read-your-writes            DynamoDB (consistent 当前 repo), Riak</span></span>
<span class="line"><span>Monotonic Reads</span></span>
<span class="line"><span>Eventual Consistency        Cassandra, Dynamo (no level tune)</span></span>
<span class="line"><span>─────────────────────────────────</span></span>
<span class="line"><span>Weak (高 throughput, 低延迟)</span></span></code></pre></div><p>图中从上到下,<em>restrictiveness 下降</em>, <em>engineering 付出下降</em>。BAT-Ocelot-OTLP 也顿美事务 SLA 支持更价el noW&quot;</p><hr><h2 id="八、一致性-vs-可用性-vs-延迟-—-sr-trade-off-矩阵" tabindex="-1">八、一致性 vs 可用性 vs 延迟 — SR Trade-off 矩阵 <a class="header-anchor" href="#八、一致性-vs-可用性-vs-延迟-—-sr-trade-off-矩阵" aria-label="Permalink to &quot;八、一致性 vs 可用性 vs 延迟 — SR Trade-off 矩阵&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Consistency</th><th>N=3 R W</th><th>例子</th><th>缺点</th></tr></thead><tbody><tr><td>Linearizable</td><td>N=3, R=3, W=2</td><td>Lock service</td><td>write 高延迟</td></tr><tr><td>Strong</td><td>N=3, R=2, W=2</td><td>Quorum with W+R&gt;N</td><td>大多 read OK</td></tr><tr><td>RYW</td><td>N=3, R=1+hint, W=2</td><td>DynamoDB session</td><td>read 高延迟</td></tr><tr><td>Eventual</td><td>N=3, R=1, W=1</td><td>Dynamo RC</td><td>时间不对</td></tr></tbody></table><h3 id="practical-r-一般公式" tabindex="-1">Practical:R = ? 一般公式: <a class="header-anchor" href="#practical-r-一般公式" aria-label="Permalink to &quot;Practical:R = ? 一般公式:&quot;">​</a></h3><p>要保证<strong>任意 quorum overlap 有最新复制</strong>, 需 <code>R + W &gt; N</code>; 若需要 <code>R = W = N</code> for linearizable 默认 + 通过 commit-token guarantee write 全部 replica 收件 → latency 高但 linearizable.</p><p>Quorum.customizable Cassandra <em>W = focal</em>.2 一_ .2 R = FOCAL_TENANT_R (=2 in standard configs). W+R=N+1+1 = 3+1=4 &gt;= 3 = N; W+R=N (W=2,R=1)<strong>不保证 overlap</strong>——可能读到一个副本其 没 updated 该 write.</p><h3 id="bounded-staleness" tabindex="-1">Bounded staleness <a class="header-anchor" href="#bounded-staleness" aria-label="Permalink to &quot;Bounded staleness&quot;">​</a></h3><p>CockroachDB / YugaByte / Spanner 都 limit staleness (e.g., 1-2s). through-put 与 linearizable close, read 行 cheaper:</p><ol><li>read at a follower, talk 心跳 cluster timestamp, follower <strong>等待</strong> 直到 crdt helper exec tang t? +动手 gain 等到 its TS 在 read end; if read can not acquire past TS within staleness, fback to quorum.</li><li><strong>Time-travel query</strong> 指定 past TS (MVCC), 取得 该 TS reader — strict replay 不 read_quorum</li></ol><hr><h2 id="九、易错清单" tabindex="-1">九、易错清单 <a class="header-anchor" href="#九、易错清单" aria-label="Permalink to &quot;九、易错清单&quot;">​</a></h2><ol><li><strong>Linearizable ≠ Serializable</strong>: 前者单操作 real-time; 后者多事务换序等价。 Spanner 是 &quot;External Consistency&quot; = 两者并存。</li><li><strong>HLC ≠ Vector Clock</strong>: HLC 单 node 节 <strong>PT+LC</strong> 但 VC 多 N node <strong>多 Counter 数组</strong>。 HLC 取 VC portability friend + fast。</li><li><strong>Sequential Consistency: 不保证 real-time.</strong> 没 HLC LC 与 VpC 的 total order-保证 二次 认 read your writes.</li><li><strong>Eventual ConsistencySLA 测</strong> eventual: 工程师口 &quot;eventually&quot; 不要视为 unbounded by SLA. DynamoDB eventual RC &lt; 1s, Cassandra 没默认 SLA。</li><li><strong>Causal Consistency 无 linearizability doesn&#39;t capture multi-version isolation 对</strong> Gallery: そこ read stale 老于因果分布 alternate ** causal Capture informacji crwrap doesn . Crummerish приемnoWfish wouldn&#39;t- solomono 判断 不能 li-nearable**.</li><li><strong>HLC cockroach-sync-action: actual necessary fixed</strong> — 因分散不同 TS atomic, 同一 physical commit may伤心 ts skew with reader.</li></ol><hr><h2 id="十、这一章带走的东西" tabindex="-1">十、这一章带走的东西 <a class="header-anchor" href="#十、这一章带走的东西" aria-label="Permalink to &quot;十、这一章带走的东西&quot;">​</a></h2><ol><li>一致性是有等级的: linearizable → sequential → causal → RYW → eventual, 由强转弱, 代价由大变小。</li><li>Vector Clock 把&quot;发生前个&quot; 形式化为 N 维 counter array, 检测各种 concurrent writes.</li><li>HLC 把 wall clock 融入 Lamport clock, 给因果关系上保与 human-friendly timestamp 一起两个。</li><li>TrueTime/Spanner 是 GPS + commit-wait 实现 external consistency, 是 linearizable+serializable 的现实极限。</li><li>ObjectTunable consistency Quorum 公式 <code>R + W &gt; N</code> 保 quorum overlap; <code>R = W = majority</code> 为最低保证 linearizable。</li><li>业务依 SLA 分级: bank → linearizable, carno-shelf → causal/RYW/internet, logs/geo … → eventual。</li></ol><hr><p>下一节 → <a href="./failure">故障检测、failure models</a></p>`,90)])])}const b=a(o,[["render",r]]);export{h as __pageData,b as default};
