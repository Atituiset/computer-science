import{_ as a,o as t,c as i,a4 as s}from"./chunks/framework.BRzJ4ijD.js";const k=JSON.parse('{"title":"ZooKeeper / etcd / Linearizability","description":"","frontmatter":{},"headers":[],"relativePath":"distributed/consensus/linearizability.md","filePath":"distributed/consensus/linearizability.md","lastUpdated":1788475713000}'),n={name:"distributed/consensus/linearizability.md"};function r(l,e,o,d,c,h){return t(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="zookeeper-etcd-linearizability" tabindex="-1">ZooKeeper / etcd / Linearizability <a class="header-anchor" href="#zookeeper-etcd-linearizability" aria-label="Permalink to &quot;ZooKeeper / etcd / Linearizability&quot;">​</a></h1><h2 id="tl-dr" tabindex="-1">TL;DR <a class="header-anchor" href="#tl-dr" aria-label="Permalink to &quot;TL;DR&quot;">​</a></h2><p><strong>ZooKeeper</strong> (Yahoo! 2008 → Apache) 与 <strong>etcd</strong> (CoreOS 2013 → CNCF) 是分布式系统协调 (coordination service) 的工业 fact 标准。共同形态: 一个 small consistent KV/znode 存储 + 复制原语 + 通知/watch API + 配置中心 + 服务发现 + leader election + 分布式锁 + 元数据存储。ZooKeeper 用 ZAB (ZooKeeper Atomic Broadcast, Paxos 派生), etcd 用 Raft。 本章梳理 ZAB 对 Paxos 的关键优化、ZK 数据模型 (znode / ephemeral / sequential / watch) 与 session 机制、etcd 数据模型 (flat KV + lease + watch stream + compaction)、二者的 linearizability 实现(以及为什么默认 ZK 读不是 linearizable, etcd 默认是)、典型误用 (double-watch, ephemeral session reset) 与生产事故。</p><hr><h2 id="一、zookeeper-架构" tabindex="-1">一、ZooKeeper 架构 <a class="header-anchor" href="#一、zookeeper-架构" aria-label="Permalink to &quot;一、ZooKeeper 架构&quot;">​</a></h2><h3 id="zab-协议" tabindex="-1">ZAB 协议 <a class="header-anchor" href="#zab-协议" aria-label="Permalink to &quot;ZAB 协议&quot;">​</a></h3><p><strong>ZAB (ZooKeeper Atomic Broadcast)</strong> 是 Flavio Junqueira 2008 设计的 Atomic Broadcast (Total Order Broadcast) 协议, 类 Paxos 但为 &quot;log replication&quot; 优化. 两个阶段:</p><ol><li><strong>Phase 1: Discovery + Synchronization (Leader Election)</strong>: <ul><li>FastLeaderElection 选 leader (bully algorithm 派生)</li><li>与所有 follower 同步: leader 把自己 last zxid 上所有 entries 复制到 followers /<strong>/</strong>,** 部分写到的 followers truncate divergent suffix**, 落后 followers 拉补议 missing entries.</li></ul></li><li><strong>Phase 2: Broadcast (Multi-Paxos-style log replication)</strong>: <ul><li>leader 接收 client write → 生成 zxid (单调增) → broadcast PROPosal 到所有 followers → majority 收到 → commit → broadcast commit.</li></ul></li></ol><h3 id="zxid" tabindex="-1">zxid <a class="header-anchor" href="#zxid" aria-label="Permalink to &quot;zxid&quot;">​</a></h3><p>zxid 是 64-bit 全序 monotonically-increasing id 由 epoch + counter 合成:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>zxid = (epoch &lt;&lt; 32) | counter</span></span></code></pre></div><p>epoch 是 每次 leader 切换自增一次; counter 每个 transaction 自增 1。 让 follower 通过 <code>zxid</code> 比较&quot;在 log 哪里&quot;, 也让 client 记 session last seen zxid 用作 failover recovery.</p><h3 id="znode-数据模型" tabindex="-1">ZNode 数据模型 <a class="header-anchor" href="#znode-数据模型" aria-label="Permalink to &quot;ZNode 数据模型&quot;">​</a></h3><p>ZK 是 <strong>hierarchical tree</strong>, 路径如 <code>/app/services/api-1/config</code>:</p><table tabindex="0"><thead><tr><th>节点类型</th><th>行为</th></tr></thead><tbody><tr><td><strong>Persistent</strong> (持久)</td><td>直到显式 delete, 客户端 disconnect 不删</td></tr><tr><td><strong>Ephemeral</strong> (临时)</td><td>客户端 session 结束自动删 (session timeout default 30-40s)</td></tr><tr><td><strong>Sequential</strong> (顺序)</td><td>创建时 ZK 在路径尾追加 monotonic counter, /leader-elec/node → /leader-elec/node00001</td></tr><tr><td><strong>Container</strong> (容器, 3.5.6+)</td><td>子节点全删后自动 delete, 避免 ephemeral 留 stale parent</td></tr><tr><td><strong>TTL</strong> (3.5.6+)</td><td>在一段时间无被修改+无子节点时自动 delete</td></tr></tbody></table><h3 id="watch-机制" tabindex="-1">Watch 机制 <a class="header-anchor" href="#watch-机制" aria-label="Permalink to &quot;Watch 机制&quot;">​</a></h3><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">zk.exists(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/lock&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">watch</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">True</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 一次性回调, 节点变化触发</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 3.6+ 引入 persistent watcher: 注册后持续触发直到移除</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">zk.addWatch(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/foo&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">persistent</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">True</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><strong>坑</strong>: 旧一次性 watch 在事件触发与 client register next watch 之间存在 race window, 可能 miss 中间变化。 Best practice: register 再 getData/getChildren (这是 &quot;double register&quot; 模式). 3.6+ PersistentWatcher 是正确做法.</p><h3 id="session-机制" tabindex="-1">Session 机制 <a class="header-anchor" href="#session-机制" aria-label="Permalink to &quot;Session 机制&quot;">​</a></h3><p>每个 client 连接有一个 <strong>session</strong>, 标识 session_id + 64bit password。 session timeout 配置(e.g., 30s). client 与 server 之间周期 ping, 任<strong>一 server</strong> 看到 session 客户端活动延长 session 的 expiry。</p><p>若 client 与 majority server 长时间断连超 timeout → session expired → 所有 ephemeral node delete → watches trigger → 业务层若用 ephemeral 模拟占锁就<strong>释放锁</strong>。 这是 ZK distributed lock 模式核心。</p><h3 id="读取-type" tabindex="-1">读取 Type <a class="header-anchor" href="#读取-type" aria-label="Permalink to &quot;读取 Type&quot;">​</a></h3><ul><li><strong>Default read</strong> from <strong>任何 follower</strong>: NOT linearizable, may return stale.</li><li><strong>Sync() before read</strong>: follower 先发 SYNC RPC 让 leader 告诉它 last committed zxid, 等 follower apply 到该 zxid 再回复 client → linearizable read。代价 ~1 extra RTT。</li></ul><hr><h2 id="二、etcd-架构" tabindex="-1">二、etcd 架构 <a class="header-anchor" href="#二、etcd-架构" aria-label="Permalink to &quot;二、etcd 架构&quot;">​</a></h2><h3 id="raft-boltdb" tabindex="-1">Raft + BoltDB <a class="header-anchor" href="#raft-boltdb" aria-label="Permalink to &quot;Raft + BoltDB&quot;">​</a></h3><p>etcd v3 用 Raft (基于 etcd-raft lib, contributors 后抽出来为 hashicorp/raft) 复制所有 KV 写到 multi-replica log, 应用组 raft log 之后 commit 到 BoltDB(纯 Go kV store, 内存 + mmap)。 etcd 存储模型是 <strong>flat KV</strong> (不是树), key 是 string, value 是 protobuf-encoded struct。</p><h3 id="lease-ttl" tabindex="-1">Lease + TTL <a class="header-anchor" href="#lease-ttl" aria-label="Permalink to &quot;Lease + TTL&quot;">​</a></h3><p>etcd v3 引入 <strong>Lease</strong>: 客户端 grant 一个 lease(分配 64-bit lease_id, TTL 例如 30s), 然后将 keys 绑到该 lease。 lease 与 ZK session 行为相似—— lease TTL 到期 no keepalive → 该 lease 上所有 keys 自动 expire。</p><p>Lease 比 session 轻——一个 client 可同时持多个 lease，把不同语义的 keys 隔离 (e.g., service discovery keys vs config keys 各一 lease)。</p><h3 id="watch-stream" tabindex="-1">Watch stream <a class="header-anchor" href="#watch-stream" aria-label="Permalink to &quot;Watch stream&quot;">​</a></h3><p>etcd v3 watch 用 <strong>gRPC server streaming</strong>:</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">watcher </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> cli.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Watch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/foo/&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, clientv3.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">WithPrefix</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> resp </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> range</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> watcher {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _, ev </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> range</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> resp.Events {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // ev.Type == PUT/DELETE</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // ev.Kv.Key, ev.Kv.Value, ev.Kv.ModRevision</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>watch 是 <strong>persistent stream</strong> (不像 ZK 一次性), 触发不会 stream 中间 watch reset, 避免一次性 watch 的 race window。 stream 内 events 包 <code>ModRevision</code> (全局 revision 单调增), client 用 <code>store.Revision</code> 收齐 previous events 至 current revision.</p><h3 id="mvcc-compaction" tabindex="-1">MVCC + Compaction <a class="header-anchor" href="#mvcc-compaction" aria-label="Permalink to &quot;MVCC + Compaction&quot;">​</a></h3><p>etcd 是 <strong>MVCC (multi-version)</strong>——每次 key 写入保存历史 revision(类似 Spanner 用 ts)。 这让 watch 能从任意历史 revision 开始 replay events。 Compaction 是后台 GC—— <code>cli.Compact(ctx, rev)</code> 删除所有 &lt; rev 的旧版本, 释放磁盘。</p><h3 id="linearizable-vs-serializable-read" tabindex="-1">Linearizable vs Serializable Read <a class="header-anchor" href="#linearizable-vs-serializable-read" aria-label="Permalink to &quot;Linearizable vs Serializable Read&quot;">​</a></h3><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 默认 Linearizable: 通过 leader / ReadIndex / Lease Read 实现</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cli.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/key&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// WithSerializable: 走 follower 本地 read, 不 consensus check, 可 stale</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cli.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ctx, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/key&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, clientv3.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">WithSerializable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">())</span></span></code></pre></div><p>默认 linearizable = etcd 用 leader + ReadIndex (1 RTT) 确认仍是 leader, 等本地 state machine apply 到 read index, 返回 client。若客户端跟 leader 不在同一集群需 follower forward 读则产生 ~3-5ms latency。</p><hr><h2 id="三、zk-vs-etcd-工程比较" tabindex="-1">三、ZK vs etcd 工程比较 <a class="header-anchor" href="#三、zk-vs-etcd-工程比较" aria-label="Permalink to &quot;三、ZK vs etcd 工程比较&quot;">​</a></h2><table tabindex="0"><thead><tr><th>维度</th><th>ZooKeeper</th><th>etcd</th></tr></thead><tbody><tr><td>协议</td><td>ZAB (Paxos variant)</td><td>Raft</td></tr><tr><td>存储模型</td><td>znode tree (hierarchical path)</td><td>flat KV (但 prefix range query)</td></tr><tr><td>API</td><td>自定义 binary, Java primary</td><td>gRPC + protobuf, multi-language</td></tr><tr><td>Watch 模型</td><td>一次性 callback (3.6+ persistent watcher)</td><td>gRPC streaming, persistent by default</td></tr><tr><td>Session 模型</td><td>TCP session</td><td>lease + TTL</td></tr><tr><td>Read 默认</td><td>Follower local read (stale)</td><td>Linearizable leader read</td></tr><tr><td>客户端代码</td><td>Java/Python/C client, ZK lib per ecosystem</td><td>Go/Java/Python/etc., gRPC stubs everywhere</td></tr><tr><td>典型部署</td><td>~5 node cluster, JVM heap ~4GB</td><td>~3-5 node cluster, 默认 2GB data disk</td></tr><tr><td>工业用</td><td>Hadoop, Kafka 旧版本, Solr, HBase leader</td><td>Kubernetes (1.20 之前) , TiKV, TiDB, Rook,不少 CNCF project</td></tr></tbody></table><h3 id="为什么-k8s-换成-etcd" tabindex="-1">为什么 K8s 换成 etcd <a class="header-anchor" href="#为什么-k8s-换成-etcd" aria-label="Permalink to &quot;为什么 K8s 换成 etcd&quot;">​</a></h3><p>ZK 用 JVM 内存大, 升级 JVM GC pause 长(~500ms), 导致 watch 重连, 偶发 5-10s service disruption. etcd 用 Go ~5MB binary + mmap 持久存, GC pause &lt;100ms, 适合 latency-sensitive Kubernetes control plane。 K8s 调用 etcd 平均不到 5ms, 远好于 ZK 的 30-100ms cluster median.</p><p>但 ZK 在 <strong>大数据生态</strong> (Hadoop、Kafka 老 / Solr Cloud / HBase master election) 仍占主流——因为生态已建立在 znode 树之上, migration 成本高, 性能也 ok。</p><hr><h2 id="四、linearizability-in-practice" tabindex="-1">四、Linearizability in Practice <a class="header-anchor" href="#四、linearizability-in-practice" aria-label="Permalink to &quot;四、Linearizability in Practice&quot;">​</a></h2><h3 id="形式-reminder" tabindex="-1">形式 reminder <a class="header-anchor" href="#形式-reminder" aria-label="Permalink to &quot;形式 reminder&quot;">​</a></h3><p>Linearizability (Herlihy-Wing 1990): 所有操作可被嵌进一个 total order, 使 (1) 顺序是 sequential-legal, (2) 与 real-time order 相符(若 op1 完成 before op2 开始)。</p><h3 id="zk-与-etcd-的实现策略" tabindex="-1">ZK 与 etcd 的实现策略 <a class="header-anchor" href="#zk-与-etcd-的实现策略" aria-label="Permalink to &quot;ZK 与 etcd 的实现策略&quot;">​</a></h3><p><strong>ZK</strong>: <strong>default read 不是 linearizable</strong>, 走 follower local state。 要 linearizable 必须 client 主动 <code>zk.sync()</code> 然后 <code>zk.get()</code> (1 extra RTT)。 多数 SDK wrapper 自动调 sync, 但 client 直 ZK API 时常忘记 → &quot;短暂读到旧值&quot;是误用常态。 ZK 3.4+ 已有线 negative caching + 非正式 linearizable read 但<strong>不</strong>AMLRT.</p><p><strong>etcd</strong>: default read <strong>linearizable</strong>, 通过 ReadIndex 实现. 代价 1 RTT (~5ms local, 30-100ms cross-DC).If client opt-in <code>WithSerializable()</code> 可走 follower read 但放弃 linearizable。</p><h3 id="distributed-lock-with-linearizability" tabindex="-1">Distributed Lock with Linearizability <a class="header-anchor" href="#distributed-lock-with-linearizability" aria-label="Permalink to &quot;Distributed Lock with Linearizability&quot;">​</a></h3><p>如果不 linearizable, distributed lock 实现 broken:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>client A: acquire lock</span></span>
<span class="line"><span>client A: GC pause 10s</span></span>
<span class="line"><span>client B: 超时 acquire lock (skip expired A lock)</span></span>
<span class="line"><span>client B: 进入 critical section</span></span>
<span class="line"><span>client A: GC resume, 仍以为持有 lock</span></span>
<span class="line"><span>→ 两个 client 同时 critical section。</span></span></code></pre></div><p>修: ZK 与 etcd 都通过 <strong>fencing token</strong> + 各 critical section 端用 token dispose 查: 后到者 violates token order, server reject write。 这种 token 在 ZK 用 ephemeral-sequential child znode 的 seqnum, etcd 用 lease + ModRevision。</p><p>Martin Kleppmann 2016 &quot;How to do distributed locking&quot; 论证 fencing token 是关键, 没 token distributed lock 不安全。</p><hr><h2 id="五、典型误用与事故" tabindex="-1">五、典型误用与事故 <a class="header-anchor" href="#五、典型误用与事故" aria-label="Permalink to &quot;五、典型误用与事故&quot;">​</a></h2><h3 id="zk-double-register-watch-event-loss" tabindex="-1">ZK Double-Register Watch Event Loss <a class="header-anchor" href="#zk-double-register-watch-event-loss" aria-label="Permalink to &quot;ZK Double-Register Watch Event Loss&quot;">​</a></h3><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">def</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> watch_callback</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(event):</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # 处理事件</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    pass</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">zk.exists(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/lock&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, watch_callback)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">zk.get(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/lock&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, watch_callback)</span></span></code></pre></div><p>两次注册同时 watch, 触发只 fire 一次, 中间 event 可能 miss (callback 内 &quot;执行业务&quot; 然后 register next watch, 但 register 期间 ZK 触发新 event 已发出但本地 callback 没 hook)。</p><p>Fix: persistent watcher (3.6+) 或 callback 同步 register。</p><h3 id="etcd-watcher-notification-loss" tabindex="-1">etcd Watcher Notification Loss <a class="header-anchor" href="#etcd-watcher-notification-loss" aria-label="Permalink to &quot;etcd Watcher Notification Loss&quot;">​</a></h3><p>etcd v3 watcher 有 <code>compacted revision</code> check —— client 启动 watch 时指定 <code>rev=N</code>, 但若 server 已 compact rev &gt; N → server returns <code>compactionRev &gt; N</code> 错误 → client 必须处理. 没处理 → silent miss events.</p><p>Fix: client 监控 compaction event, 加 rewatch from currentRev + 业务级 reconciliation。</p><h3 id="zk-session-reset-storm-during-gcp-maintenance" tabindex="-1">ZK Session Reset Storm during GCP Maintenance <a class="header-anchor" href="#zk-session-reset-storm-during-gcp-maintenance" aria-label="Permalink to &quot;ZK Session Reset Storm during GCP Maintenance&quot;">​</a></h3><p>2021 GCP 维护期间 ZK leader 重启且 follower 网络抖动, 多个 client session 触发 reset, ephemeral nodes 大批 delete, leader election 触发 stampede——服务 dev machine 不在平分钟 de Leader elect expon为 backoff 后 渡过. Fix: client SDK 加 randomized election retry + jitter.</p><h3 id="etcd-mvcc-database-space-exceeded" tabindex="-1">etcd mvcc: database space exceeded <a class="header-anchor" href="#etcd-mvcc-database-space-exceeded" aria-label="Permalink to &quot;etcd mvcc: database space exceeded&quot;">​</a></h3><p>etcd 默认 2GB data backend, 长期写入但<strong>没定期 compaction</strong> → 满磁盘 → 拒收 new writes → cluster 进入 paused 状态. Fix: cron compaction (<code>etcdctl compact $rev; etcdctl defrag</code>), 或 <code>-auto-compaction-retention=1h</code> 自动 compact.</p><h3 id="k8s-api-server-stw-timeout-waiting-for-etcd" tabindex="-1">K8s API Server &quot;STW: timeout waiting for etcd&quot; <a class="header-anchor" href="#k8s-api-server-stw-timeout-waiting-for-etcd" aria-label="Permalink to &quot;K8s API Server &quot;STW: timeout waiting for etcd&quot;&quot;">​</a></h3><p>etcd cluster quorum 失(2 of 3 down) → K8s API server 阻塞所有写(Pod schedule、ReplicaSet 都 stop)。 多个 K8s 集群挂报告有关 etcd 的 root cause, 推荐<strong>低 fsync latency NVMe</strong> disk + <strong>足够大 heap / high net bandwidth</strong> + <strong>不要在 low-IOPS disk 上跑 etcd</strong>—— disk IO 是 etcd 实际 P99 latency 决定因.</p><hr><h2 id="六、易错清单" tabindex="-1">六、易错清单 <a class="header-anchor" href="#六、易错清单" aria-label="Permalink to &quot;六、易错清单&quot;">​</a></h2><ol><li><strong>ZK read 默认是 follower-local stale, 不是 linearizable</strong> — 必须 <code>sync()</code> 再读, 多数业务 case 不在意 stale 才可弊。 一定业务用 fenced.</li><li><strong>ZK ephemeral session 不是客户端 single conn</strong> — session 跟 ZK quorum 持久, client 切 connection 不 reset session; 直到真 timeout(默认 30-40s)才 expire。</li><li><strong>etcd 默认 KV 是 MVCC</strong> — Watch 必须 catch compaction 错误(<code>&lt;rev compacted</code>), 否则 silent miss。</li><li><strong>etcd lease 续租必须 heartbeat ignore failure</strong> — gRPC client 必须周期 <code>lease.KeepAlive()</code>, 超过 TTL lease 自动 expire + keys 删, 业务回滚。</li><li><strong>Distributed lock 没 fencing token 不安全</strong> — even with linearizability, long GC pause 仍可双 client 进 critical。 fencing token (znode seq / ModRevision) + critical section side verify 是 must.</li><li><strong>ZK watch 一次性</strong> — 旧 API trigger 后必须 re-register, re-register 与新 event 间有 race; 用 3.6+ persistent watcher 或 careful double-register。</li><li><strong>etcd linearizable read 不是 0-RTT</strong> — 默认是 1 RTT leader check, cross-DC 部署时必算这个进 SLA。</li><li><strong>multi-region ZK/etcd 慎重</strong> — 跨洲 ping 200-300ms, leader lease renew 需要 cross-DC heartbeats, 业务抖动会触发不必要 election。可用 multi-cluster + cross-replicate (MirrorMaker-like) 不直接 multi-DC ZK。</li><li><strong>ZK 5 节点不是&quot;5个 9 可用&quot;</strong> — true availability 取决于 election timeout、客户端 retry、long GC pause → client sessionId. 5 节点让你忍受 2 节点死但仍写。</li></ol><hr><h2 id="七、这一章带走的东西" tabindex="-1">七、这一章带走的东西 <a class="header-anchor" href="#七、这一章带走的东西" aria-label="Permalink to &quot;七、这一章带走的东西&quot;">​</a></h2><ol><li>ZK 用 ZAB (类 Paxos), etcd 用 Raft; ZK-生态大数据为主、 etcd-生态 CNCF/K8s 为主。</li><li>ZK 数据是路径树 + ephemeral + sequential + watch; etcd 是 flat KV + lease + persistent watch stream + MVCC。</li><li>ZK default read 是 follower-local(stale, 需 <code>sync()</code>), etcd default linearizable (1 RTT ReadIndex)。</li><li>etcd MVCC: watch 必须 handle compaction error, 否则 silent event lost。</li><li>Distributed lock 必带 fencing token (ZK ephemeral-sequential seq num / etcd ModRevision) 防止双 client 因 GC pause 同时进 critical section。</li><li>Linearizability 是默认承诺(etcd) vs 外加承诺(ZK)的工程取舍, 影响测试用例 baseline。</li><li>集群 quota / disk IO / GC pause 是 etcd 在 P99 下破裂的根本; ZK 是 JVM 重启损失.</li></ol><hr><p>下一节 → <a href="./../replication/">复制</a></p>`,80)])])}const g=a(n,[["render",r]]);export{k as __pageData,g as default};
